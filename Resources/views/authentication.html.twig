<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8"/>
    <title>{% block title %}Pronto - App management{% endblock %}</title>

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    {% block stylesheets %}
        <link href="{{ mix('/css/login.css') }}" rel="stylesheet"/>
    {% endblock %}

    <link rel="icon" type="image/x-icon" href="favicon.ico"/>

    {% if customer is defined and customer != null %}
        <link rel="stylesheet" type="text/css" href="/bundles/prontomobile/styles/customer.php?primaryColor={{ customer.primaryColor }}&secondaryColor={{ customer.secondaryColor }}&sidebarColor={{ customer.sidebarColor }}"/>
    {% endif %}
</head>
<body>

{% block body %}

{% endblock %}

<footer class="authentication-footer">
    Pronto is a product by <a class="esites" target="_blank" href="https://www.e-sites.nl">E-sites</a>.<br>
    &copy; {{ "now"|date("Y") }}
</footer>
<svg xmlns="http://www.w3.org/2000/svg" version="1.1">
    <defs>
        <filter id="blur">
            <feGaussianBlur stdDeviation="5"/>
        </filter>
    </defs>
</svg>

{% block javascripts %}
    <script src="{{ mix('/js/app.js') }}"></script>
    <script type="text/javascript" src="{{ mix('/js/login.js') }}"></script>
{% endblock %}
</body>
</html>
